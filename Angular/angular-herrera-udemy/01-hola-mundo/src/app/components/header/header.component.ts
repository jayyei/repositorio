import { Component } from '@angular/core';

@Component({
  selector: 'app-header', //Nombre que se le da al componente y que es visible al momento de insertar su tag HTML
  templateUrl: './header.component.html' //Plantilla o contenido HTML de nuestro componente HTML
})
export class HeaderComponent {

}
