	/// Protocolo: un especie de acuerdo o contrato, un estandar 

	//HTTP es un estandar para que los servidores y las paginas se han logrado comunicar entre si, se basa en un esquema de petición-respuesta 
		entre cliente y servidor

	//HTTP hyperText Transfer protocol y HTTPS es hyperText Transfer protocol SECURE

	//HTTPS es una variante de HTTP que añade una capa de seguridad extra al encriptar datos a traves de SSL/TLS esto es un protocolo de seguridad

	//Un servlet no es mas que un manejador de peticiones http

	//El tipo de contenido content-type es lo que el servidor devuelve al cliente, puede ser json, text/html, xml entre otros

	//Cualquier maquina o computador puede ser un servidor: suele tener mucha velocidad de procesador, ram, etc.

	//Un servidor web es un programa que esta instalado dentro del so, algunos de ellos son Apache que permite albergar y procesar paginas con php
	// IIS, ngnix, glashfish, Tomcat, JBOss, weblogic

	//El servidor web, es el que puede responder e interpretar peticiones, este servidor web se encuentra dentro de un servidor

	//El servidor web tiene dos contenedores (para el caso de java, ie glashFish) un contenedor de servlets y otro de ejb's

	//Tomcat, no permite contener ejb's

	//Aunque es una clase java, los servlets estan mapeados o relacionados a unas url en las cuales se aceptan las peticiones
